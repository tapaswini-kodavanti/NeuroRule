## EVOTER: Evolution of Transparent Explainable Rule-sets

Configuration files are located in the `evolution/domain` directory. Each domain is explained in detail in the [paper](https://arxiv.org/abs/2204.10438).

### Running Evolution
To start the evolution process, run the following command:

```bash
python evolution/app/evolve.py -p evolution/domain/path_to_config